<h1>Liste des Tracteurs neufs</h1>

<br />
<div class="search">
	<%= search_form_for @search do |f|%>
		<div class="field">
		    <%= f.label :modele_cont, "Modele est :" %>
		    <%= f.text_field :modele_cont %>
	  	</div>
	  	<div class="actions">
	    	<%= f.submit "Rechercher" %>
	  	</div>
	<% end %>
</div>

<br />
<%= link_to 'Nouveau Tracteur', new_tracteur_neuf_path %>
<br />
<%= link_to 'Retour', acceuil_index_path %>
<br />
<br />

<table class="table">
  <tr>
    <th>Emplacement</th>
    <th>Stock</th>
    <th>Famille</th>
    <th>Modele</th>
    <th>Description</th>
    <th>Prix</th>
    <th>Bonus</th>
    <th>Arrivee</th>
    <th>Facture</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tracteur_neufs.each do |tracteur_neuf| %>
  <tr>
    <td><%= tracteur_neuf.emplacement %></td>
    <td><%= tracteur_neuf.stock %></td>
    <td><%= tracteur_neuf.famille %></td>
    <td><%= tracteur_neuf.modele %></td>
    <td><%= tracteur_neuf.description %></td>
    <td><%= tracteur_neuf.prix %></td>
    <td><%= tracteur_neuf.bonus %></td>
    <td><%= tracteur_neuf.arrivee %></td>
    <td><%= tracteur_neuf.facture %></td>
	<% if can? :read, @article %>
		<td><%= link_to 'Show', tracteur_neuf %></td>
	<% end %>
	<% if can? :update, @article %>
		<td><%= link_to 'Edit', edit_tracteur_neuf_path(tracteur_neuf) %></td>
	<% end %>
	<% if can? :delete, @article %>
		<td><%= link_to 'Destroy', tracteur_neuf, method: :delete, data: { confirm: 'Êtes-vous sure?' } %></td>
	<% end %>
  </tr>
<% end %>
</table>
