<h1>Rental Fleet CKL</h1>

<br />
<%- if can? :create, RentalFleet2 %>
	<%= link_to 'Nouvelle Unite', new_rental_fleet2_path %>
<% end %>

<br />
<table class="table">
  <tr>
    <th>Modele</th>
    <th># Serie</th>
    <th>Client</th>
    <th>Location</th>
    <th>Date de Sortie</th>
    <th>Vendeur</th>
    <th>Heures</th>
    <th>Bonus</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @rental_fleet2s.each do |rental_fleet2| %>
  <tr>
    <td><%= rental_fleet2.model %></td>
    <td><%= rental_fleet2.serial %></td>
    <td><%= rental_fleet2.client %></td>
    <td><%= rental_fleet2.location %></td>
    <td><%= rental_fleet2.dateout %></td>
    <td><%= rental_fleet2.salesman %></td>
    <td><%= rental_fleet2.hours %></td>
    <td><%= number_to_currency(rental_fleet2.bonus) %></td>
    <td><%= link_to 'Details', rental_fleet2 %></td>
    <%- if can? :update, RentalFleet2 %>
    	<td><%= link_to 'Modifier', edit_rental_fleet2_path(rental_fleet2) %></td>
    <% end %>
    <%- if can? :destroy, RentalFleet2 %>
    	<td><%= link_to 'Supprimer', rental_fleet2, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  	<% end %>
  </tr>
<% end %>
</table>


